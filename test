// 1. 
Give me code of let, var and const with the explained in comment 

// 2. 
explain this how its work const person = { fname: "John", lname: "Doe", age: 25 };
let text = "";
for (let x in person) {
  text += person[x] + " ";
}
console.log(text); // john Doe 25

// 3.
explain the return keyword in function and how its work in js? 
if i am not using return what is the disadvantages?
function loginUserMessage(userName) {
    return `${userName} Just Logged in`;
  }
loginUserMessage("Deep Chandra Joshi");


// 4.